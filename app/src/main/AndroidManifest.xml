<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:hardwareAccelerated="true"
        android:usesCleartextTraffic="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:name=".CodeCatApplication"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Material3.Dark.NoActionBar"
        tools:targetApi="31">
        <activity
            android:name=".activities.mainactivity.MainActivity"
            android:exported="true"
            android:theme="@style/Launcher">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".activities.loginactivity.LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="com.github.krxwl.codecat.LoginActivity" />

                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>
        <activity
            android:name=".activities.registrationactivity.RegistrationActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="com.github.krxwl.codecat.RegistrationActivity" />
            </intent-filter>
        </activity>
        <activity
            android:name=".activities.taskactivity.TaskActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="com.github.krxwl.codecat.RegistrationActivity" />
            </intent-filter>
        </activity>
    </application>

</manifest>